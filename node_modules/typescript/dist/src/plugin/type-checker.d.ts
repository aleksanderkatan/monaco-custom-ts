/**
 * TypeScript program manager with caching
 */
import ts from 'tsover';
export interface ProgramManagerOptions {
    tsconfigPath?: string;
}
export declare class ProgramManager {
    private program;
    private configPath;
    private options;
    private host;
    constructor(options?: ProgramManagerOptions);
    private resolveConfigPath;
    private loadCompilerOptions;
    private createProgram;
    getProgram(): ts.Program;
    getTypeChecker(): ts.TypeChecker;
    getSourceFile(fileName: string): ts.SourceFile | undefined;
    refresh(): void;
    destroy(): void;
}
/**
 * Get or create a cached program manager
 */
export declare function getProgramManager(options?: ProgramManagerOptions): ProgramManager;
//# sourceMappingURL=type-checker.d.ts.map